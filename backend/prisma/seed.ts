import { PrismaClient } from "@prisma/client";
const prisma = new PrismaClient();
async function main() { await prisma.issue.upsert({ where: { publicId: "CIV-1024" }, update: {}, create: { publicId: "CIV-1024", category: "POTHOLE", imageUrl: "https://images.unsplash.com/photo-1558618666-fcd25c85cd64?auto=format&fit=crop&w=1200&q=85", latitude: 28.6139, longitude: 77.209, address: "Outer Ring Road, Sector 15", description: "Large pothole near the main road entrance.", severity: 8.7, confidence: 96, priority: 92, reportCount: 12, uniqueReporterCount: 8, status: "IN_PROGRESS", assignedTeam: "Road Maintenance", aiSummary: "Potential road hazard detected from the supplied evidence.", statusEvents: { create: [{ status: "REPORTED", actor: "citizen", note: "Issue submitted" }, { status: "AI_ANALYZED", actor: "system", note: "Preliminary AI estimate completed" }, { status: "IN_PROGRESS", actor: "operations", note: "Road Maintenance started work" }] } } }); }
main().then(() => prisma.$disconnect()).catch(async (error) => { console.error(error); await prisma.$disconnect(); process.exit(1); });
